VITE_BACKEND_URL=https://album-backend-259509076903.us-central1.run.app
VITE_CLIENT_TOKEN=<gerar com `openssl rand -hex 24` e setar igual no Cloud Run env CLIENT_TOKEN>
# Esse arquivo é só template — o real (.env.production) está no .gitignore.
# O VITE_CLIENT_TOKEN vai pro bundle público; serve apenas pra evitar bots
# casuais. Proteção real é via --max-instances=2 no Cloud Run e billing alert.
